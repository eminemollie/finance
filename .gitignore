# pull_latest.exe 打包產生的檔案，只存在本機，不需要進repo
pull_latest.exe
build_tmp/
build_log.txt

# pull_latest.py 執行時自己產生的記錄檔，只是本機除錯用
pull_latest_log.txt

# 舊版偵錯留下的暫存輸出，不需要進repo
result.txt

# Excel開啟財務管理系統.xlsx時自動產生的鎖定暫存檔，只存在本機，
# 不小心在Excel還開著的時候commit可能會把這個也帶進去，先排除掉
~$*.xlsx

# Python快取
__pycache__/
*.pyc
